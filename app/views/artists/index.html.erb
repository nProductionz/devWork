<p id="notice"><%= notice %></p>

<h1>Elenco Artisti</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Genre</th>
      <th>About</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @artists.each do |artist| %>
      <tr>
        <td><%= artist.name %></td>
        <td><%= artist.genre %></td>
        <td><%= artist.about %></td>
        <td><%= link_to 'Show', artist %></td>
        <td><%= link_to 'Destroy', artist, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<p>
  <%= link_to 'Diventa un\'Artista', new_artist_path %>
  <br>
  <%= link_to 'Elenco Requests', requests_path %>
  <br>
</p>
<p><br></p>
<br>